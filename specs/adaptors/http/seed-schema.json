{
  "openapi": "3.0.3",
  "info": {
    "title": "http — mocker seed-data schema",
    "version": "1.0.0",
    "description": "Schema for the seed data openfn-mocker needs to impersonate http. Derived from the adaptor's OpenAPI spec. A seed dump is { collection: { id: record } }; each record validates against its resource schema below.",
    "x-openfn-adaptor": "http",
    "x-derived-from": "openapi.json",
    "x-captured-at": "2026-07-06"
  },
  "x-seed": {
    "shape": "{ collection: { id: record } }",
    "collections": {
      "deleteResults": {
        "resource": "DeleteResult",
        "schema": {
          "$ref": "#/components/schemas/DeleteResult"
        }
      },
      "resources": {
        "resource": "Resource",
        "schema": {
          "$ref": "#/components/schemas/Resource"
        }
      }
    }
  },
  "components": {
    "schemas": {
      "SeedData": {
        "type": "object",
        "description": "Full seed dump loaded by mocker: one property per collection.",
        "properties": {
          "deleteResults": {
            "type": "object",
            "description": "Records keyed by id for the deleteResults collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/DeleteResult"
            }
          },
          "resources": {
            "type": "object",
            "description": "Records keyed by id for the resources collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/Resource"
            }
          }
        },
        "additionalProperties": false
      },
      "Resource": {
        "type": "object",
        "description": "A generic JSON resource. Concrete fields depend on the target API.",
        "properties": {
          "id": {
            "type": "string",
            "example": "1"
          },
          "name": {
            "type": "string",
            "example": "example"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          }
        },
        "additionalProperties": true
      },
      "DeleteResult": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "example": "1"
          },
          "deleted": {
            "type": "boolean",
            "example": true
          }
        }
      }
    }
  }
}
