{
  "openapi": "3.0.3",
  "info": {
    "title": "common — mocker seed-data schema",
    "version": "1.0.0",
    "description": "Schema for the seed data openfn-mocker needs to impersonate common. Derived from the adaptor's OpenAPI spec. A seed dump is { collection: { id: record } }; each record validates against its resource schema below.",
    "x-openfn-adaptor": "common",
    "x-derived-from": "openapi.json",
    "x-captured-at": "2026-07-06"
  },
  "x-seed": {
    "shape": "{ collection: { id: record } }",
    "collections": {
      "resources": {
        "resource": "Resource",
        "schema": {
          "$ref": "#/components/schemas/Resource"
        }
      },
      "responses": {
        "resource": "Response",
        "schema": {
          "$ref": "#/components/schemas/Response"
        }
      }
    }
  },
  "components": {
    "schemas": {
      "SeedData": {
        "type": "object",
        "description": "Full seed dump loaded by mocker: one property per collection.",
        "properties": {
          "resources": {
            "type": "object",
            "description": "Records keyed by id for the resources collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/Resource"
            }
          },
          "responses": {
            "type": "object",
            "description": "Records keyed by id for the responses collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/Response"
            }
          }
        },
        "additionalProperties": false
      },
      "Resource": {
        "type": "object",
        "description": "A generic REST resource: an id plus arbitrary additional fields. common does not fix any vendor schema.",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the resource."
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": true
      },
      "ResourceList": {
        "type": "object",
        "description": "Envelope for a collection of generic resources.",
        "properties": {
          "data": {
            "type": "array",
            "description": "The list of resources.",
            "items": {
              "$ref": "#/components/schemas/Resource"
            }
          },
          "count": {
            "type": "integer",
            "description": "Number of resources returned."
          }
        },
        "required": [
          "data"
        ]
      },
      "Response": {
        "type": "object",
        "description": "Generic HTTP response wrapper mirroring what common's http helpers return on state.",
        "properties": {
          "body": {
            "description": "The parsed response body (arbitrary JSON).",
            "nullable": true
          },
          "statusCode": {
            "type": "integer",
            "description": "HTTP status code of the response."
          },
          "headers": {
            "type": "object",
            "description": "Response headers as key/value pairs.",
            "additionalProperties": {
              "type": "string"
            }
          }
        },
        "required": [
          "body",
          "statusCode",
          "headers"
        ]
      }
    }
  }
}
