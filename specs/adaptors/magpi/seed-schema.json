{
  "openapi": "3.0.3",
  "info": {
    "title": "magpi — mocker seed-data schema",
    "version": "1.0.0",
    "description": "Schema for the seed data openfn-mocker needs to impersonate magpi. Derived from the adaptor's OpenAPI spec. A seed dump is { collection: { id: record } }; each record validates against its resource schema below.",
    "x-openfn-adaptor": "magpi",
    "x-derived-from": "openapi.json",
    "x-captured-at": "2026-07-06"
  },
  "x-seed": {
    "shape": "{ collection: { id: record } }",
    "collections": {
      "surveyDataLists": {
        "resource": "SurveyDataList",
        "schema": {
          "$ref": "#/components/schemas/SurveyDataList"
        }
      },
      "uploadResponses": {
        "resource": "UploadResponse",
        "schema": {
          "$ref": "#/components/schemas/UploadResponse"
        }
      }
    }
  },
  "components": {
    "schemas": {
      "SeedData": {
        "type": "object",
        "description": "Full seed dump loaded by mocker: one property per collection.",
        "properties": {
          "surveyDataLists": {
            "type": "object",
            "description": "Records keyed by id for the surveyDataLists collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/SurveyDataList"
            }
          },
          "uploadResponses": {
            "type": "object",
            "description": "Records keyed by id for the uploadResponses collection.",
            "additionalProperties": {
              "$ref": "#/components/schemas/UploadResponse"
            }
          }
        },
        "additionalProperties": false
      },
      "SurveyDataRequest": {
        "type": "object",
        "description": "Form-encoded request parameters for the Get Survey Data endpoints.",
        "required": [
          "username",
          "accesstoken",
          "surveyid"
        ],
        "properties": {
          "username": {
            "type": "string",
            "description": "Magpi username that owns the access token.",
            "example": "testuser"
          },
          "accesstoken": {
            "type": "string",
            "description": "API access token generated for the user.",
            "example": "yt72xk536RUPpKscMUof"
          },
          "surveyid": {
            "type": "string",
            "description": "Identifier of the survey/form whose data is being fetched.",
            "example": "37479"
          },
          "startdate": {
            "type": "string",
            "format": "date",
            "description": "Only return submissions on or after this date (must be used with enddate).",
            "example": "2017-09-27"
          },
          "enddate": {
            "type": "string",
            "format": "date",
            "description": "Only return submissions on or before this date (must be used with startdate).",
            "example": "2017-10-27"
          },
          "start": {
            "type": "integer",
            "description": "Pagination offset (index of first record to return).",
            "example": 0
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of records to return.",
            "example": 100
          }
        }
      },
      "SurveyDataList": {
        "type": "object",
        "description": "Container returned by the Get Survey Data endpoints (XML SurveyDataList, converted to JSON).",
        "properties": {
          "SurveyData": {
            "type": "array",
            "description": "Submission records. Upstream XML may return a single object or an array; the adaptor normalises to an array.",
            "items": {
              "$ref": "#/components/schemas/SurveyData"
            }
          }
        }
      },
      "SurveyData": {
        "type": "object",
        "description": "A single survey submission. Answer fields are dynamic per form, so arbitrary string-valued properties are permitted alongside the well-known metadata fields.",
        "properties": {
          "SubmissionId": {
            "type": "string",
            "description": "Unique identifier of the submission.",
            "example": "9911002"
          },
          "SubmissionDate": {
            "type": "string",
            "description": "Timestamp the submission was received.",
            "example": "2017-09-28 14:03:11"
          },
          "LastSubmissionDate": {
            "type": "string",
            "description": "Timestamp of the most recent submission in the list; the adaptor stores this as state.lastSubmissionDate for incremental fetches.",
            "example": "2017-10-01 09:15:00"
          },
          "surveyId": {
            "type": "string",
            "description": "Survey identifier, stamped onto each record by the adaptor.",
            "example": "37479"
          },
          "source": {
            "type": "string",
            "description": "Source label stamped onto each record by the adaptor.",
            "example": "Magpi API"
          }
        },
        "additionalProperties": {
          "type": "string",
          "description": "Form-specific answer values keyed by question/field name."
        }
      },
      "RecordUpload": {
        "type": "object",
        "description": "A record to submit to an existing form. The adaptor serialises this JSON object into an XML <form> element. Field names correspond to the target form's questions.",
        "properties": {
          "surveyid": {
            "type": "string",
            "description": "Identifier of the form/survey to submit against.",
            "example": "37479"
          },
          "data": {
            "type": "object",
            "description": "Answer values keyed by field name.",
            "additionalProperties": {
              "type": "string"
            }
          }
        },
        "additionalProperties": true
      },
      "UploadResponse": {
        "type": "object",
        "description": "Result of an inbound uploadData submission.",
        "properties": {
          "status": {
            "type": "string",
            "description": "Outcome status of the upload.",
            "example": "success"
          },
          "recordId": {
            "type": "string",
            "description": "Identifier assigned to the newly created record.",
            "example": "9911003"
          },
          "message": {
            "type": "string",
            "description": "Human-readable result message.",
            "example": "Record uploaded successfully."
          }
        }
      }
    }
  }
}
